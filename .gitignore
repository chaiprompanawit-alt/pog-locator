# deps
/node_modules
/.pnp
.pnp.*
# lock ไม่ commit — Vercel resolve เองตอน build เหมือนที่ผ่านมา
package-lock.json

# next
/.next/
/out/
next-env.d.ts

# env (ห้าม commit ค่าจริง)
.env
.env.local
.env*.local

# misc
.DS_Store
*.log
/coverage
