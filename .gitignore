# deps
/node_modules
/.pnp
.pnp.*
# lock ไม่ commit — Vercel resolve เองตอน build เหมือนที่ผ่านมา
package-lock.json

# next
/.next/
/out/
next-env.d.ts
tsconfig.tsbuildinfo

# env (ห้าม commit ค่าจริง)
.env
.env.local
.env*.local

# รูปตัวอย่างป้ายชั้นที่ถ่ายหน้างาน — repo นี้ public ไม่เอาขึ้น
/example

# misc
.DS_Store
*.log
/coverage
